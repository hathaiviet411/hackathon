# Base URL of the deployed backend (frontend and backend deploy separately).
# Leave empty for local dev — requests fall back to same-origin relative paths,
# served by the mock API dev plugin (vite-plugins/mockApi.ts).
# Example once backend is live: VITE_API_BASE_URL=https://api.your-backend.com
VITE_API_BASE_URL=
